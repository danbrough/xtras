package org.danbrough.openssl.plugin

import org.gradle.api.Plugin
import org.gradle.api.Project

class OpenSSLPlugin : Plugin<Project> {
  override fun apply(target: Project) {
   // target.logInfo("applying OpenSSLPlugin")
  }
}